import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.harvester.app',
  appName: 'Harvester',
  webDir: 'dist/ng-harvester/browser',
};

export default config;
